<div class="container">
	<div class="row">
		<div class="w12 wysiwyg">
			<h1>Events</h1>
			<% _.each(events, function(thisEvent) { %>
			<div data-id="<%= thisEvent.id %>" data-model="event">
				<p><%= thisEvent.eventTitle %></p>
				<p>The event is organised by <%= thisEvent.eventTeam.teamName %></p>			
			</div>
			<% }) %>
		</div>
	</div>
</div>